# SamLife Environment Variables

# Supabase Cloud Sync (Optional - for cloud backup)
# Get these from: https://supabase.com/dashboard (Create free project)
# 1. Go to supabase.com and create a new project
# 2. Go to Settings > API
# 3. Copy the URL and anon key below

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# If you want cloud sync, create these tables in Supabase SQL Editor:
# 
# CREATE TABLE user_data (
#   id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
#   user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE,
#   data_type TEXT NOT NULL,
#   data JSONB NOT NULL,
#   updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
#   UNIQUE(user_id, data_type)
# );
# 
# ALTER TABLE user_data ENABLE ROW LEVEL SECURITY;
# 
# CREATE POLICY "Users can manage their own data"
#   ON user_data
#   FOR ALL
#   USING (auth.uid() = user_id);
